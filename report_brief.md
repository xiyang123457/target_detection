# 小数据迁移中全参微调的负效应：VOC 上 Faster R-CNN 的对照实验

> **项目**：在 Pascal VOC 2012 上微调 COCO 预训练的 Faster R-CNN (ResNet50-FPN)
> **日期**：2026-09-21 ｜ **完整实验记录**：见 [`result.md`](./result.md) ｜ **复现命令**：见 [`README.md`](./README.md)
> **评估口径**：torchmetrics `MeanAveragePrecision`（pycocotools 101 点插值），val 前 1000 张固定子集，`skip_difficult=True`

---

## 摘要

COCO 预训练的 Faster R-CNN 在 VOC 上**零样本**（不做任何训练）mAP@0.5 已达 **0.76**。

- **全参微调**（41.2M 参数）训练 18 轮后降至 **0.61** —— **比不训练低 15.4 个点**，且 **18/20 个类退化**。
- **仅训练分类头**（0.26% 参数）可得 **0.77**，但只比零样本高 **0.68 个点**。

**结论**：在类别空间完全重叠的迁移场景下，微调几乎没有增益空间，而**全参微调是净损害**。

---

## 1. 问题

迁移学习的默认动作是：加载预训练模型 → 微调 → 涨点。但在**小数据集**（VOC train 仅 5717 张图）上，这个默认动作是否总成立？

本实验回答三个问题：

1. COCO 预训练特征本身有多强？（需要一个零样本锚点）
2. 微调究竟带来了多少**净**增益？
3. 冻结策略（全参 vs 只换头）如何影响结果？

> **为什么这个问题值得问**：如果"微调"在小数据上是负收益，那么"先微调再说"就是一个需要被质疑的默认动作。这一点对所有小样本迁移任务都成立，不限于检测。

---

## 2. 假设与证伪条件

| | 假设 | 证伪条件 |
|---|---|---|
| **H1**（主） | 在 VOC 这种小数据上，全参微调会因破坏预训练特征而**劣于**仅训练分类头 | 若全参微调的**峰值** mAP ≥ 只换头的 mAP，H1 被推翻 |
| **H2** | COCO 预训练模型在 VOC 上零样本即可达到可用精度（因 20/20 类完全重叠） | 若零样本 mAP@0.5 < 0.30，H2 被推翻 |

**两个假设都给出了可被现有数据推翻的条件**，不存在事后解释空间。这是本实验设计的一部分。

---

## 3. 实验设计

| 配置 | 可训练参数 | 占比 | lr | epoch | 说明 |
|---|---|---|---|---|---|
| `zero` | 0 | 0% | — | — | COCO 权重直接推理 VOC，不做任何训练 |
| `head` | 107,625 | **0.26%** | 0.001 | 8 | 冻结全部，只训 `roi_heads.box_predictor` |
| `head_lr5e3` | 107,625 | 0.26% | 0.005 | 待补 | lr 敏感性对照 |
| `full` | 41,396,536 | **100%** | 0.005 | 0–18 | torchvision 默认（仅冻结 `conv1` + `layer1`）|

**受控条件**（四项全部一致，保证可比）：

- 同一 val 前 **1000 张**（`VOCDataset` 对文件名 `sorted()`，故每次评的是同一批图）
- `min_size=480` / `max_size=800`
- `skip_difficult=True`、`model.eval()` + `no_grad`、不传 `targets`
- 同一指标实现（`torchmetrics`，零样本脚本复用 `evaluate.py` 的 `save_metrics` / `collect_per_class`）

**未受控**：`head` 与 `full` 的 lr 不同（0.001 vs 0.005）—— 见第 6 节第 1 条。

---

## 4. 结果

### 4.1 主结果

| 配置 | mAP@0.5 | mAP@0.5:0.95 | 相对零样本 |
|---|---|---|---|
| `zero`（不训练） | **0.7635** | 0.5238 | — |
| `head` @ep8 | **0.7703** | 0.5232 | **+0.68 点** |
| `full` @ep0（峰值） | 0.7137 | 0.4295 | **−4.98 点** |
| `full` @ep18 | 0.6093 | 0.3640 | **−15.42 点** |

![mAP-epoch 曲线](assets/fig_map_curve_n1000.png)

**图 1**：灰虚线为零样本水平线。`full` 全程位于零样本**之下**，且随训练推进持续下降。

### 4.2 全参微调的训练曲线：单调下降

| epoch | 0 | 3 | 6 | 9 | 12 | 15 | 18 |
|---|---|---|---|---|---|---|---|
| **mAP@0.5** | 0.7137 | 0.7090 | 0.6465 | 0.6685 | 0.6577 | 0.6396 | **0.6093** |
| **mAP@0.5:0.95** | 0.4295 | 0.4390 | 0.3916 | 0.4154 | 0.3941 | 0.3773 | **0.3640** |
| train `loss_classifier` | 0.1688 | — | — | 0.0789 | — | — | **0.0715** |

**关键对比**：train loss 从 0.1688 降到 0.0715（**降 58%**），而 mAP@0.5 从 0.7137 降到 0.6093（**降 10.4 点**）。

> **训练目标与泛化性能方向相反** —— 这是泛化 gap 的直接证据，而不是"训练不充分"。
> 注意：全参微调的峰值出现在 **epoch 0**（训练 1 轮之后），此后一路下降。

### 4.3 逐类分析

| | 退化类数（相对零样本） | 平均增益 |
|---|---|---|
| `full` | **18 / 20** | **−4.98 点** |
| `head` | 9 / 20 | +0.68 点 |

`full` 仅在 `bottle`（+6.1）和 `car`（+5.8）两类上有提升；`head` 在 **19/20** 类上高于 `full`。

![逐类 AP 差值](assets/fig_per_class_delta_n1000.png)

**图 2**：红色为微调后反而退化的类。`full` 几乎全红。

| 类 | zero | full | head | full 相对 zero |
|---|---|---|---|---|
| motorbike | 0.948 | 0.791 | **0.943** | **−15.7** |
| cow | 0.777 | 0.651 | 0.749 | −12.6 |
| bicycle | 0.877 | 0.753 | **0.874** | −12.4 |
| train | 0.888 | 0.771 | **0.886** | −11.7 |
| bird | 0.882 | 0.801 | **0.897** | −8.1 |
| person | 0.927 | 0.907 | **0.933** | −2.0 |
| car | 0.795 | **0.853** | **0.888** | +5.8 |

**注意 `head` 那一列**：只换头几乎把每一类都"恢复"到零样本水平，而全参微调把它们全部拉低。
这不是"某几类出问题"，而是**全局性的特征破坏**。

---

## 5. 机制解释

**① 类别空间完全重叠 → 零样本本就很强。**
VOC 的 20 个类在 COCO 的 90 类中 **100% 有对应**（`aeroplane`→`airplane`、`motorbike`→`motorcycle`、`tvmonitor`→`tv` 等同义映射）。
COCO 模型已经"认识"全部 VOC 类，缺的只是编号对齐。**没有新的语义需要学。**

**② 预训练特征的可线性分离性很高 → 微调没有增益空间。**
只换头模式下，`loss_classifier` 在 **epoch 3** 就降到 0.100，之后 15 轮几乎不动（0.100 → 0.0985）。
线性探针在 3 轮内即饱和，说明 1024 维 ROI 特征已经把 VOC 的 20 类分得很开。

**③ 41.2M 参数在 5717 张图上更新 → 灾难性遗忘。**
梯度噪声主导更新方向，预训练学到的通用特征被覆盖。证据是 **train loss 持续下降而 mAP 持续下降**（第 4.2 节）。
若只是"没学好"，train loss 应当不降；若只是"学错了"，mAP 应当先升后降 —— 实测是**从第 1 轮之后就开始降**。

---

## 6. 局限（诚实声明）

1. **学习率未受控**。`full` 用 0.005，`head` 用 0.001。官方 `lr=0.005` 是给 8 卡 × batch=2（全局 batch 16）配的，本实验单卡 batch=4，理论缩放值应约 0.00125 —— **全参微调的 lr 可能偏大 4 倍**。
   间接证据：`head` 在 lr=0.001 与 0.005 下 loss 轨迹几乎重合（0.0985 vs 0.1000），说明该结构对 lr 不敏感。但**全参微调对 lr 的敏感性本就高于线性探针**，不能完全外推。**补 `full + lr=0.001` 是首要的下一步。**
2. **单种子，无误差棒**。所有数字仅 1 次运行（seed=42）。故只报 2 位小数；**结论只押"趋势与符号"（曲线是否下降、逐类差值方向），不押小数点**。判断规则：差异 > 3 点才下结论，< 2 点降级为"可比、需多种子确认"。
3. **评估规模 n=1000**，是按文件名排序的前 1000 张，非全量 5823，可能对某些类有偏。需全量复验。
4. **零样本类别映射未人工核对**。`VOC_TO_COCO` 为硬编码映射，0.7635 偏高，需逐类核对并全量复验。
5. **无独立 hold-out**。val 同时用于选择 epoch 和报告结果，数字偏乐观。
6. **只测了一个数据集对**（COCO→VOC）。结论是否推广到类别重叠度低的任务，未验证。

---

## 7. 下一步

1. **补 `full + lr=0.001`（6 epoch）** —— 排除学习率混淆，这是唯一可能推翻主结论的实验
2. **全量 5823 复验**主结果（当前是 n=1000 子集）
3. **3 种子**报 mean ± std
4. **缩小子数据集**（如 1000 张）观察退化是否加剧 —— 若加剧，则直接支持"小数据导致遗忘"的机制
5. **手写 VOC 11 点 AP** 与 torchmetrics 对拍，确认指标实现无误
6. **换一个类别重叠度低的数据集**（如具身场景小数据集），检验结论的边界

---

## 附：结果可信度自查

| 检查项 | 做法 | 结果 |
|---|---|---|
| 数据泄漏 | VOC 官方 train/val 划分，按文件名天然不重叠 | ✅ |
| 测试集污染 | 预训练权重来自 COCO，与 VOC 无交集 | ✅ |
| 评估模式 | `model.eval()` + `no_grad` + 不传 `targets` | ✅ |
| 三方口径一致 | 同一 `n=1000` 子集、同一 `min_size`、同一指标实现 | ✅ |
| 逐类均值 vs overall | 三个来源均对得上（差 < 0.001） | ✅ |
| 零样本映射 | 硬编码，**未人工核对** | ⚠️ |
| 随机性 | 单种子，无 std | ❌ |
| 独立 hold-out | 无，val 兼作报告集 | ❌ |
